# Research Testcase (Archived)

This document described legacy v0/dual-agent long-run tests for "LLM writes code + run in sandbox".
That code path has been removed to keep the repository focused on the **scheme phase**.

For current usage:
- Scheme blueprint: `docs/experiments/scheme_phase/blueprint.md`
- Run autonomous scheme agent: `python scripts/run_scheme_agent.py ...`

